import React from 'react';
import { AboutContent } from '@/components/about/AboutContent';

export const metadata = {
  title: 'SoundBridge — About',
  description: 'Learn how SoundBridge connects your favorite music with Korean traditional gugak.',
};

export default function AboutPage() {
  return <AboutContent />;
}
